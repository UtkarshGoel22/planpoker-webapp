export enum Deck {
  SERIAL = "SERIAL",
  EVEN = "EVEN",
  ODD = "ODD",
  FIBONACCI = "FIBONACCI",
}

export enum InviteStatus {
  ACCEPTED = "ACCEPTED",
  PENDING = "PENDING",
  REJECTED = "REJECTED",
}

export enum PokerBoardStatus {
  CREATED = "CREATED",
  STARTED = "GAME_STARTED",
  ENDED = "GAME_ENDED",
}
